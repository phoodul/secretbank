---
description: Run tests and build Tauri release binary
---

// turbo

1. Run `npm run test`
   // turbo
2. Run `cargo test`
   // turbo
3. Run `cargo clippy -- -D warnings`
   // turbo
4. Run `cargo tauri build`
5. Report output paths for each platform bundle.
